# Cloud Run 移植のための事前計測

## これは何か

`roomplanner-web`（3D部屋模様替えアプリ）の家具生成を、外部APIではなく
**自前で動かす**ための準備。このリポジトリを GCP の Cloud Run（GPU）に載せる前に、
ローカルのGPUで生成時間とVRAM使用量を測る。

### なぜ測るのか

Cloud Run のGPUは**秒課金**なので、`生成時間 × 単価` がそのまま費用になる。
見積もりの不確かさは「実際の生成時間」の一点に集約されていた。

**ローカルでの計測は実施済み**（結果は後述）。残っているのは Cloud Run 上での
コールドスタートを含む実測で、これはデプロイ後に測る。

## 背景：なぜ自前で動かすのか

外部APIは値上げ・仕様変更・廃止がこちらの都合と無関係に起きるため、
長期的な管理コストが高い。実際に以下が起きている。

- RunPod のエンドポイントが知らぬ間に削除されていた
- Tripo は V2 API を 2026年11月に停止する
- 旧実装は Synexa という外部APIに依存していたが、コードを読むまで気づけなかった

## 手順

### 1. セットアップ

README の指定バージョンに**厳密に**従うこと。ここを外すとCUDA拡張のビルドが通らない。

```bash
conda create -n hy3d python==3.10.9
conda activate hy3d

pip install torch==2.5.1 torchvision torchaudio \
  --index-url https://download.pytorch.org/whl/test/cu124
pip install -r requirements.txt

# CUDA拡張のビルド（ここが山場）
cd hy3dgen/texgen/custom_rasterizer       && python3 setup.py install && cd ../../..
cd hy3dgen/texgen/differentiable_renderer && python3 setup.py install && cd ../../..
```

`setup.py install` は setuptools 80 以降で削除されている。失敗する場合は
`pip install "setuptools<80"` で固定するか、以下に置き換える。
`--no-build-isolation` は必須（拡張が setup.py の中で torch を import するため）。

```bash
pip install --no-build-isolation ./hy3dgen/texgen/custom_rasterizer
pip install --no-build-isolation ./hy3dgen/texgen/differentiable_renderer
```

### 2. 計測

```bash
bash measure.sh
```

profile 3 / 2 / 1 × 形状のみ / テクスチャ付き の**6パターン**を実行し、
所要時間とVRAM最大値を `measure_result.txt` に記録する。
初回はモデルの重みをダウンロードするため時間がかかる。

別の画像で測りたい場合は引数で渡す。

```bash
bash measure.sh path/to/image.png
```

### 3. 記録する数字

`measure_result.txt` の内容をそのまま残す。特に重要なのは以下。

| 項目 | 用途 |
|---|---|
| GPU名とVRAM | L4への換算係数を出す |
| profileごとの所要時間 | 秒課金なので直接コストになる |
| VRAM最大値 | L4の24GBに収まる profile を決める |
| HuggingFaceキャッシュのサイズ | イメージサイズとコールドスタートの見積もり |

## profile の意味

```
1 = HighRAM_HighVRAM     ← 最速
2 = HighRAM_LowVRAM
3 = LowRAM_HighVRAM      ← 既定（VRAM 9GB想定）
4 = LowRAM_LowVRAM       （6GB以上）
5 = VerylowRAM_LowVRAM
```

mmgp は「VRAMが足りない環境で動かす」ための仕組みで、重みをCPUとGPUの間で
退避させる代わりに時間がかかる。手元のPCなら「遅くても動く」のが正義だが、
**Cloud Run は秒課金なので遅い＝高い**。

L4 の 24GB は丸ごと確保されて課金されるので、使い切ったほうが得。
既定の 3 は VRAM 9GB 前提なので、もっと速い側（1〜2）に振れるはず。

**ただし実測では profile 3 で VRAM 6.5GB しか使っておらず、VRAMが制約に
なっていない**（後述）。profile を上げても速度が改善するとは限らない。
テクスチャ生成の修正後に測ったのは profile 3 のみで、1・2 は未測定。

## ビルドで詰まったときによくある原因

- `nvcc` が見つからない → CUDA Toolkit の導入が必要
- torch の CUDA バージョンと、システムの CUDA バージョンが不一致
- `ninja` が入っていない
- C++ コンパイラのバージョンが古い

エラーログは捨てずに残すこと（`measure_*.log` に出力される）。

## 実測結果（2026-09-07）

RTX 4070 12GB / profile 3 / `assets/example_images/052.png`

| ケース | 所要時間 | VRAM最大 |
|---|---|---|
| 形状のみ | 41.6 秒 | 6,557 MiB |
| テクスチャ付き | **98.5 秒** | 6,546 MiB |

テクスチャ付きの内訳:

| 工程 | 時間 |
|---|---|
| モデルロード | 約 30 秒 |
| 形状生成 | 14.5 秒 |
| 面数削減 | 7.1 秒 |
| テクスチャ生成 | **46.4 秒** |
| 正味処理（ロードを除く） | **68 秒** |

出力: 26,575頂点 / 40,000面 / 2048px テクスチャ / 4.8MB

モデルの重み: **約20GB**（形状 7.2GB ＋ テクスチャ 12.7GB）

### コンテナでの実測（2026-09-08）

| 工程 | ホスト | コンテナ |
|---|---|---|
| 形状生成 | 14.5 秒 | 14.6 秒 |
| 面数削減 | 7.1 秒 | 7.0 秒 |
| テクスチャ生成 | 46.4 秒 | 45.6 秒 |
| 全体 | 98.5 秒 | 125.0 秒 |

**各工程はホストとほぼ一致しており、コンテナ化による速度低下はない。**
全体の差（約26秒）はイメージの起動と重みの読み込みで、Cloud Run でも
同じ構造になる。

イメージサイズは **14.6GB**（重みを含まない）。

### 実測から分かったこと

**① VRAMは6.5GBしか使っていない**

L4 の 24GB を大幅に余らせている。profile 3（VRAM 9GB想定）のままで収まるので、
より速い profile を試す余地はあるが、VRAMが制約になっていない以上、
速度改善は限定的かもしれない。

むしろ注目すべきは、1インスタンスに3生成分の余裕があること。同時実行できれば
コストが1/2〜1/3になる。ただし mmgp のオフロードと pinned memory が絡むため、
スレッド安全性の検証が必要。**将来の最適化候補。**

**② テクスチャ生成が形状の3倍のコスト**（46.4秒 vs 14.5秒）

コストが問題になったとき、「形状のみを既定、テクスチャは任意」という
製品側の選択肢が効く。重みも 7.2GB まで落ちるのでコールドスタートも改善する。

**③ 重み20GBがイメージ構成を決める**

Cloud Run のイメージストリーミングは10GB未満で有効だが、超えると逆に遅くなる。
重みはイメージに入れず、起動時に GCS から並列ダウンロードする方針
（詳細は `DOCKER.md`）。

**④ 重みを外に出してもイメージが 14.6GB ある**

内訳の大半は CUDA の devel イメージ（ツールキット一式）と torch。
**重みを外に出したのに、イメージ単体で10GBの閾値を超えている。**

マルチステージビルド（拡張を devel でビルドし、成果物だけ runtime イメージへ
コピーする）で CUDAツールキット一式を落とせる。**次に着手する課題。**

## L4 への換算と月額見積もり

RTX 4070 と L4 は演算性能がほぼ同等だが、**メモリ帯域が L4 は約6割**
（300 GB/s vs 504 GB/s）。拡散モデルの推論は帯域の影響を受けるため、
L4 のほうが遅くなる。

| | 4070 実測 | L4 推定 |
|---|---|---|
| 正味処理 | 68 秒 | **75〜95 秒** |
| モデルロード | 30 秒 | 60〜180 秒（20GBをGCSから読む） |

L4 の単価は GPU 単体ではなく、必須の 4vCPU / 16GiB を含めて
**$0.000323/秒（＝$1.16/時）**。

| | 1生成あたり | 月500生成 |
|---|---|---|
| 温まっている状態 | **$0.027** | $14 |
| コールドスタート込み | **$0.066** | **$33** |

利用が散発的だとほぼ毎回コールドスタートになるため、後者で見ておくのが安全。

### 月額合計（ユーザー100人・月500生成）

| 項目 | 月額 |
|---|---|
| Cloud Run GPU（L4） | $33 |
| Artifact Registry（イメージ **14.6GB** 実測） | **$1.5** |
| Cloud Storage（重み20GB + 生成物） | $0.5 |
| 下り通信 | $0.5 |
| Cloud Run CPU / Firestore / Cloud Tasks | $0（無料枠） |
| **合計** | **約$36** |

イメージサイズは当初 3〜5GB と見込んでいたが、実測は 14.6GB だった。
費用への影響は月$1程度で小さいが、**コールドスタートへの影響のほうが問題**
（上記④）。マルチステージビルドで削る。

**まだ推定が残っている箇所**: L4での生成時間（4070からの外挿）と、
GCSからの20GB読み込み時間。どちらもデプロイ後に実測して確定させる。

## 次にやること

1. ~~実測値で月額見積もりを引き直す~~ 完了
2. ~~Dockerfile を書く~~ 完了（`DOCKER.md` 参照）
3. ~~ローカルのDockerでビルドと実行を確認する~~ 完了（コンテナ化のコストはゼロと確認）
4. **マルチステージビルドでイメージを削る（現状14.6GB → 10GB未満を狙う）** ← 現在ここ
5. HTTPサーバ化（`api_server.py` の `POST /send` → `GET /status/{uid}` を起点にする）
6. 起動時に GCS から重みを取得する処理を足す
7. Cloud Run（GPU L4）にデプロイし、コールドスタートを含む実費用を測る
8. API層を作る（Cloud Run CPU + Cloud Tasks + Firestore + Cloud Storage）
